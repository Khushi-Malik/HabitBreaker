import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            ZStack {
                VStack {
                    Text("HabitBreaker.")
                        .font(.title)
                    
                }
            }
            Spacer()
        }
    }
}

#Preview {
    ContentView()
}
